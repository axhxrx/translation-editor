/**
 Random intial value to avoid slow loading of 30,000 entries on every load. (User can click clear button if they really wanna see all.)
 */
export const randomInitialSearchValue = 'Flux'; /* [
  'Napter',
  'Beam',
  'Lagoon',
  'Soracam',
  'Flux'
][Math.floor(Math.random() * 5)]*/